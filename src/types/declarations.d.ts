declare module '@vitejs/plugin-react-swc';
declare module 'lovable-tagger';

// Provide __dirname for editor type checking (Node types are installed, but some configs may still complain)
declare const __dirname: string;
